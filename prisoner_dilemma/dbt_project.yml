name: 'prisoner_dilemma'
version: '1.0.0'
config-version: 2

profile: 'prisoner_dilemma'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  prisoner_dilemma:
    gold:
      +materialized: table
      +schema: gold
